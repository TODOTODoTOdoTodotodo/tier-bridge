# 📊 analyze_usage.py 사용 및 분석 가이드

이 문서는 TierBridge 하네스 프록시의 실행 로그(`harness.log`)를 분석하여 토큰 소모량, **생성 소스코드 라인 수(LOC)**, 모델 라우팅 등급별 분포, 그리고 예상 소모 비용(USD)을 일자별/기간별로 정량화하여 산출해주는 **`analyze_usage.py`** 헬퍼 스크립트의 상세 가이드 문서입니다.

---

## 📋 목차
1. [1. 개요 및 목적](#1-개요-및-목적)
2. [2. 주요 특징](#2-주요-특징)
3. [3. 사용 방법 (Command Usage)](#3-사용-방법-command-usage)
4. [4. 분석 리포트 결과 항목 설명](#4-분석-리포트-결과-항목-설명)
5. [5. ROI 측정 및 실무 활용 팁](#5-roi-측정-및-실무-활용-팁)
6. [6. 자주 묻는 질문 (FAQ)](#6-자주-묻는-질문-faq)

---

## 1. 개요 및 목적

Codex Enterprise CLI를 로컬 우회 모드(`--oss --local-provider=ollama`)로 가동할 경우 CLI 자체 `status` 명령어로는 계정 크레딧 및 소모 토큰 정보가 표시되지 않습니다.

`analyze_usage.py`는 프록시가 `harness.log` 파일에 실시간으로 기록하는 타임스탬프 기반의 **`➔ [USAGE]`** 이벤트 라인을 정밀 파싱하여, **누적 사용량, 작성 소스코드 라인 수(LOC) 및 일자별 비용 절감 지표를 자동으로 리포트** 형태로 집계합니다.

---

## 2. 주요 특징

* **자동 타임스탬프 파싱**: 로그 라인에 기록된 `[YYYY-MM-DD HH:MM:SS]` 시간을 인식하여 날짜별 사용량을 자동 수집합니다.
* **실시간 생성 소스코드(LOC) 집계**: 모델 답변 내 마크다운 코드 블록(` ``` `) 라인을 추출하여 개발 생산성 지표(LOC)를 파싱합니다.
* **유연한 날짜 필터링**: `--date YYYY-MM-DD` 옵션을 통해 특정 일자의 사용량만 선택하여 정밀 모니터링할 수 있습니다.
* **하위 호환성 유지**: 타임스탬프 또는 LOC 필드가 없는 구형 로그 형식도 예외 없이 호환하여 유실 없이 파싱합니다.
* **엔터프라이즈 크레딧(Enterprise Credits) 자동 산출**: 달러 비용(`cost_usd`)을 기반으로 Codex Enterprise 크레딧 환산 단가(`1 Credit = $0.20 USD`)를 적용하여 예상 차감 크레딧 수치를 일자별/등급별로 함께 리포팅합니다.
* **3-Tier / 4-Tier 동적 등급 소모 분포 제공**: `LUNA:LOW`, `LUNA:MEDIUM`, `TERRA:MEDIUM`, `TERRA:HIGH`, `TERRA:EXTRA_HIGH`, `SOL:EXTRA_HIGH` 등 등급별 소모 비용, 토큰 비율 및 작성 LOC를 명확하게 구분해 표시합니다.

---

## 3. 사용 방법 (Command Usage)

### 3.1. 기본 전체 사용량 분석
프로젝트 루트 디렉토리에서 기본 `harness.log` 파일의 전체 누적 통계를 조회합니다.

```bash
./analyze_usage.py
```
*(또는 `python3 analyze_usage.py`)*

### 3.2. 특정 날짜 필터링 조회 (`--date` / `-d`)
원하는 특정 날짜(예: `2026-07-27`)의 소모량 및 등급 분포만 추출하여 조회합니다.

```bash
./analyze_usage.py --date 2026-07-27
# 축약형
./analyze_usage.py -d 2026-07-27
```

### 3.3. 특정 로그 파일 지정 분석
기본 `harness.log` 외에 백업해 둔 다른 로그 파일(예: `logs/harness_2026_07.log`)을 지정하여 분석합니다.

```bash
./analyze_usage.py logs/harness_2026_07.log
./analyze_usage.py logs/harness_2026_07.log --date 2026-07-27
```

---

## 4. 분석 리포트 결과 항목 설명

실행 시 아래와 같이 정돈된 CLI 요약 표가 출력을 전개합니다.

```text
====================================================================================================
📊 [TierBridge 로그 기반 USAGE 사용량 및 소스코드 작성(LOC) 통계 보고서]
====================================================================================================
🗓️  조회 대상 날짜: 2026-08-03
📁 대상 로그 파일: harness.log
📈 총 성공 요청 수 (Requests) : 10 회
📥 총 Input 토큰 수          : 500,861 tokens
📤 총 Output 토큰 수         : 1,625 tokens
🔢 전체 총 소모 토큰 수       : 502,486 tokens
💻 총 작성/생성 코드 (LOC)    : 0 lines
💰 총 추정 소모 비용         : $0.586019 USD
💳 총 추정 소모 크레딧       : 2.93 Credits (1 Credit = $0.20 USD)
----------------------------------------------------------------------------------------------------

[1] 🎯 등급(Decision)별 소모 분포
Decision 등급        | 요청 수     | Input 토큰     | Output 토큰    | 코드 (LOC)   | 비용 (USD)    | 예상 크레딧 (Credits)
----------------------------------------------------------------------------------------------------
LUNA:MEDIUM        | 8        | 393,143      | 1,272        | 0          | $0.396959   | 1.98 Credits
TERRA:MEDIUM       | 1        | 52,542       | 210          | 0          | $0.133455   | 0.67 Credits
LUNA:LOW           | 1        | 55,176       | 143          | 0          | $0.055605   | 0.28 Credits

[2] 🗓️  일자(Daily)별 소모 요약
날짜           | 요청 수     | Input 토큰     | Output 토큰    | 코드 (LOC)   | 비용 (USD)    | 예상 크레딧 (Credits)
--------------------------------------------------------------------------------------------------
2026-08-03   | 10       | 500,861      | 1,625        | 0          | $0.586019   | 2.93 Credits
====================================================================================================
```

### 세부 항목 안내:
1. **총 성공 요청 수 (Requests)**: 프록시를 통해 업스트림 API로 성공적으로 전송되어 토큰이 소모된 요청 횟수.
2. **Input / Output 토큰 수**: 프롬프트 입력 토큰 수와 모델 생성 답변 토큰 수의 각각 합산.
3. **총 작성/생성 코드 (LOC)**: 마크다운 코드 블록 내에서 생성된 실질 소스코드 줄 수.
4. **총 추정 소모 비용 (USD)**: 모델 카탈로그 단가(1M 토큰당 가격)를 기준으로 산출된 실질 소모 예상 가치.
5. **총 추정 소모 크레딧 (Credits)**: 달러 환산 소모 비용을 기반으로 계산된 Codex Enterprise 계정 예상 크레딧 소모량 (`$0.20 USD = 1 Credit`).
5. **[1] 등급별 소모 분포**: `LUNA:LOW`(기본 저비용) 대 `TERRA` 모델의 비중 및 등급별 생산된 LOC 확인.
6. **[2] 일자별 소모 요약**: 날짜별 요청 수, 코드 생산성(LOC) 및 소모액의 추이를 일목요연하게 비교.

---

## 5. ROI 측정 및 실무 활용 팁

1. **사내 비용 및 개발 생산성 보고서 작성 시**:
   * 주간/월간 단위로 `./analyze_usage.py`를 실행한 출력을 기반으로 `LUNA` 모델 비중(통상 **70% 이상 유지가 이상적**) 및 생성 코드 라인 수(LOC)를 확인하여 장표에 작성할 수 있습니다.
2. **크레딧 소모율 모니터링**:
   * 회사 한도(월 1,000 크레딧 등) 대비 일자별 평균 소모액을 체크하여 월말 과소모 발생 위험을 사전에 파악합니다.

---

## 6. 자주 묻는 질문 (FAQ)

### Q1. 분석 결과에 `⚠️ 분석할 [USAGE] 로그 레코드가 없습니다.` 가 출력됩니다.
* **원인**: 지정한 로그 파일 내에 `➔ [USAGE]` 이벤트 라인이 없거나, 지정한 날짜(`--date`)에 해당하는 통계 데이터가 없는 경우입니다.
* **해결**: `tail -n 20 harness.log`를 실행하여 프록시가 실제 요청을 받아 토큰 수집 로그를 남겼는지 확인하십시오.

### Q2. 프록시 프로세스를 재부팅해도 이전 로그 통계가 보존되나요?
* **네, 완벽히 보존됩니다.** `/usage` HTTP 엔드포인트는 메모리 기반이지만, `analyze_usage.py`는 디스크 파일인 `harness.log`에 저장된 텍스트 데이터를 직접 읽어 파싱하므로 서버 재시작이나 컴퓨터 재부팅 후에도 과거 기록이 100% 보존되어 통계를 산출할 수 있습니다.
