# 📊 analyze_usage.py 가이드 문서

이 문서는 TierBridge 하네스 프록시의 실행 로그(`harness.log`)를 분석하여 토큰 소모량, **생성 소스코드 라인 수(LOC)**, 모델 라우팅 등급별 분포, 그리고 예상 소모 비용(USD)을 일자별/기간별로 정량화하여 산출해주는 **`analyze_usage.py`** 헬퍼 스크립트의 가이드 문서입니다.

---

## 📋 목차
1. [1. 개요 및 목적](#1-개요-및-목적)
2. [2. 사용 방법 (Command Usage)](#2-사용-방법-command-usage)
3. [3. 분석 리포트 항목 설명](#3-분석-리포트-항목-설명)
4. [4. 실무 활용 팁](#4-실무-활용-팁)

---

## 1. 개요 및 목적

`analyze_usage.py`는 프록시가 `harness.log` 파일에 실시간으로 기록하는 타임스탬프 기반의 **`➔ [USAGE]`** 이벤트를 정밀 파싱하여 **누적 토큰 사용량, 생성 코드 라인 수(LOC), 등급별 비율, 일자별 비용 추이**를 한눈에 볼 수 있는 요약 리포트로 출력해줍니다.

서버나 컴퓨터를 재부팅하더라도 디스크 로그 파일(`harness.log`)에 누적 기록되므로 과거 통계가 영구적으로 보존됩니다.

---

## 2. 사용 방법 (Command Usage)

### 2.1. 기본 전체 사용량 분석
프로젝트 루트 디렉토리에서 아래 명령어를 실행하면 `harness.log` 전체 누적 통계, 월별/세션별 요약 및 Top 프롬프트 인사이트를 조회합니다.

```bash
./analyze_usage.py
```
*(또는 `python3 analyze_usage.py`)*

### 2.2. 월별(Monthly) 기간 조회 (`--month` / `-m`)
특정 월(예: `2026-08`)의 통계 및 소모 요약만 조회합니다.

```bash
./analyze_usage.py --month 2026-08
# 축약형
./analyze_usage.py -m 2026-08
```

### 2.3. 세션 ID(Session ID) 필터링 조회 (`--session` / `-s`)
특정 대화 세션 ID(예: `5eb61a1e`)의 소모 토큰 및 크레딧만 정밀 파싱합니다.

```bash
./analyze_usage.py --session 5eb61a1e
# 축약형
./analyze_usage.py -s 5eb61a1e
```

### 2.4. Kibana 스타일 웹 대시보드 생성 및 열기 (`--html` / `-w`)
Kibana 풍의 아름다운 반응형 다크테마 시각화 리포트 웹페이지 (`usage_dashboard.html`)를 생성하고 자동으로 브라우저에 엽니다.

```bash
./analyze_usage.py --html
./analyze_usage.py --month 2026-08 --html
```

### 2.5. 특정 날짜 필터링 조회 (`--date` / `-d`)
특정 날짜(예: `2026-08-14`)의 소모량만 추출하여 조회합니다.

```bash
./analyze_usage.py --date 2026-08-14
# 축약형
./analyze_usage.py -d 2026-08-14
```

### 2.6. 특정 로그 파일 지정 분석
백업해 둔 별도의 로그 파일(예: `harness.log.bak`)을 지정하여 분석할 수 있습니다.

```bash
./analyze_usage.py harness.log.bak
./analyze_usage.py harness.log.bak --date 2026-08-14
```

---

## 3. 분석 리포트 항목 설명

```text
=========================================================================================
📊 [TierBridge 로그 기반 USAGE 사용량 및 소스코드 작성(LOC) 통계 보고서]
=========================================================================================
🗓️  조회 대상 월: 2026-08
📁 대상 로그 파일: harness.log
📈 총 성공 요청 수 (Requests) : 103 회
📥 총 Input 토큰 수          : 6,926,979 tokens
📤 총 Output 토큰 수         : 38,410 tokens
🔢 전체 총 소모 토큰 수       : 6,965,389 tokens
💻 총 작성/생성 코드 (LOC)    : 60 lines
💰 총 추정 소모 비용         : $10.502365 USD
💳 총 추정 소모 크레딧       : 52.51 Credits (1 Credit = $0.20 USD)
-----------------------------------------------------------------------------------------

[1] 🎯 등급(Decision)별 소모 분포
Decision 등급        | 요청 수     | Input 토큰     | Output 토큰    | 코드 (LOC)   | 비용 (USD)     | 예상 크레딧 (Credits)
---------------------------------------------------------------------------------------------------------
GOLD               | 37       | 2,243,822    | 13,489       | 0          | $5.744445   | 28.72 Credits
BRONZE             | 44       | 3,030,233    | 19,326       | 60         | $3.088211   | 15.44 Credits
SILVER             | 22       | 1,652,924    | 5,595        | 0          | $1.669709   | 8.35 Credits

[2] 🗓️  일자(Daily)별 소모 요약
날짜           | 요청 수     | Input 토큰     | Output 토큰    | 코드 (LOC)   | 비용 (USD)     | 예상 크레딧 (Credits)
-----------------------------------------------------------------------------------------------
2026-08-14   | 12       | 853,447      | 3,623        | 0          | $0.951253   | 4.76 Credits
=========================================================================================
```

* **총 성공 요청 수**: 업스트림 API 전송 성공 횟수
* **Input / Output / Total 토큰 수**: 소모된 프롬프트 및 답변 토큰 누적 합계
* **총 작성/생성 코드 (LOC)**: 응답 마크다운 코드 블록(` ``` `) 내에서 자동 파싱된 실질 소스코드 줄 수
* **총 추정 소모 비용 (USD)**: 모델 단가(1M 토큰당 가격) 대입 소모액
* **총 추정 소모 크레딧 (Credits)**: `$0.20 USD = 1 Credit` 환산치
* **[1] 등급별 소모 분포**: `BRONZE`, `SILVER`, `GOLD`, `PLATINUM`, `DIAMOND`, `CHALLENGER` 랭크별 소모 비중 및 등급별 생성 코드 라인 수
* **[2] 일자별 소모 요약**: 날짜별 요청 수, 코드 생산성(LOC) 및 비용 추이 비교

---

## 4. 실무 활용 팁

1. **사내 주간/월간 비용 및 코드 생산성 산출**:
   * 매주/매월 `./analyze_usage.py`를 실행하여 등급별 소모액 및 생성 코드 라인 수(LOC) 데이터를 보고 장표에 활용할 수 있습니다.
2. **크레딧 과소모 예방**:
   * 일자별 평균 소모액을 체크하여 월 1,000 크레딧 한도 내 유지 여부를 지속적으로 모니터링합니다.
